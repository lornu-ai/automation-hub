{
  "name": "automation-hub",
  "version": "0.1.0",
  "description": "Cloudflare Worker-based CI automation service.",
  "scripts": {
    "build": "bun run tsc",
    "azure:build": "bun run src/azure_eks_build.ts"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "bun-types": "^1.3.5",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "jose": "^6.1.3"
  }
}
